# --- Core Application Framework ---
# Das Herzstück deiner Webanwendung
flask
flask-cors
flask-jwt-extended
flask-migrate
python-dotenv         # Zum Laden von .env-Dateien

# --- Datenbank ---
# SQLAlchemy für die ORM-Schicht, Alembic für Migrationen
sqlalchemy
alembic

# --- Asynchrone Tasks (laut Projektplan) ---
# Celery für die Task Queue, [redis] installiert den nötigen Treiber mit
celery[redis]

# --- Externe APIs & Services ---
# Clients für die LLM-Dienste
openai
anthropic

# --- Datenvalidierung & Serialisierung ---
pydantic

# --- Hilfsprogramme & weitere wichtige Pakete ---
bcrypt                # Zum sicheren Hashen von Passwörtern
pillow                # Zur Bildverarbeitung
python-ulid           # Für eindeutige, sortierbare IDs
httpx                 # Moderner HTTP-Client (ersetzt requests, kann auch async)